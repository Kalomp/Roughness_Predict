# Roughness_Predict
x_test and x_train is the input to the network model, where each row is a sequence and every third row is a set of data.
y_test and y_train is the output of the network model, each row is a set of data, the first column is the initial roughness and the second column is the post-throw roughness.
